<div class="wrapper">
  <div class="container">

    <div class="logo"></div>

     <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name), :class => 'form') do |f| %>

          <%= f.input :email, required: false, autofocus: true, placeholder: 'Adresse e-mail' %>
          <%= f.input :password, required: false, placeholder: 'Mot de passe' %>
          <br><center>
     <%= f.submit "Valider", :class => 'bouton' %>
     <% end %>
     <div class="separateur"></div>
     <%= link_to new_user_registration_path do %>
     <div class="bouton" style="background-color:#2BC4BE;color:white;">Créer un compte</div>
     <% end %>

  </div>

</div>

